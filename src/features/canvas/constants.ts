const canvasWidth = 1800;
const canvasHeight = 1800;
const backgroundStyle: CanvasFillStrokeStyles["fillStyle"] = "#ffffff";

export const canvasConfig = {
  canvasWidth,
  canvasHeight,
  backgroundStyle,
} as const;
